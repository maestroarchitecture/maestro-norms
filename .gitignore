# Python
__pycache__/
*.pyc
.venv/
venv/

# Embeddings / artefacts key-gated (volumineux, reproductibles)
*.emb.jsonl
index/embeddings.npy
index/embeddings_ids.json

# Secrets — ne JAMAIS committer de clé
.env
*.key
probes.json

# DTU/NF sous licence (REEF) — stockage INTERNE, jamais publié (repo public).
# Pivot .md + chunks de DTU payants = consultation interne / retrieval seulement.
private/

# OS
.DS_Store
